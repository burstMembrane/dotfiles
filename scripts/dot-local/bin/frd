#!/bin/zsh
# Use fzf to select a directory
fzf \
  --ansi \
  --tmux 90% \
  --preview-window=right:70% \
  --preview 'tree -L 2 {1}' \
  --reverse \
  --with-nth 2.. \
  --bind "start:reload: fd -t d --maxdepth 1 | xargs stat -f '%a %N' | sort -nr" \
  | awk '{print $2}'
\builtin cd "$dir"
